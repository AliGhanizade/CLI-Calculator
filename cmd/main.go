package main

import "calculator/calculator"

func main() {
	// This calculator works with arguments, so you need to give it arguments after execution. 
	//for example : go run main.go 2 + 2 
	//go run main.go 50 ^ 5
	//go run main.go 36 / 2
	calculator.Calculator()
}